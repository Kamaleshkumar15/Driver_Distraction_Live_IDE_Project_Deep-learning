Put YOLOv7 weights here.

Expected optional file:

yolov7.pt

For your custom model:

best.pt

The live program automatically continues without YOLO if weights are missing, so face/eye/drowsiness monitoring can still be demonstrated from the laptop camera.
